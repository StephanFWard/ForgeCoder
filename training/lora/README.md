# training/lora

PEFT adapter checkpoints and prepared datasets live here (gitignored).

Outputs:
- `train.jsonl` / `eval.jsonl` / `manifest.json` — from `prepare_dataset.py`
- `checkpoints/` — from `train_lora.py`