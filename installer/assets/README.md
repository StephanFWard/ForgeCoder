# installer/assets

- `forge.ico` — application icon (place a 256×256 icon here before building).
- Any wizard images used by `ForgeCoder.iss`.

All assets are referenced from `ForgeCoder.iss` via `assets\`.